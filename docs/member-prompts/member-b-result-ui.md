# 成员 B 初始提示词

你当前工作在 `feature/result-ui`。只允许修改 `frontend/src/features/conditions/`、`frontend/src/features/verification/`、`frontend/src/features/comparison/`、`frontend/src/components/dynamic-form/` 和 `frontend/src/components/evidence/`。先阅读 README、API 合同和团队所有权。

本次任务：完善动态条件表单、加载/空/错误状态、结果三栏、证据详情和横评占位 UI。字段必须来自 category profile，禁止写死具体品类字段或修改 API 合同。

完成后运行 TypeScript 检查和构建，输出修改文件、测试结果、未完成项和风险。禁止修改其他成员目录或整体重构。

