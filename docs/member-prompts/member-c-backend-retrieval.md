# 成员 C 初始提示词

你当前工作在 `feature/backend-retrieval`。只允许修改 `backend/app/api/`、`backend/app/database/`、`backend/app/models/`、`backend/app/schemas/`、`backend/app/services/retrieval/`、`backend/tests/`、`data/real/` 和数据脚本。先阅读 README、API 合同和团队所有权。

本次任务：完善 FastAPI、SQLite 预留、证据检索接口和测试。输入输出使用 Pydantic，错误返回统一格式，外部调用有超时和异常处理。不得自行修改 Mock 数据结构或 API 字段。

完成后运行 pytest 和基础导入检查，输出修改文件、测试结果、未完成项和风险。禁止修改其他成员目录或整体重构。

