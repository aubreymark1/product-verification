# 种草验真 AI 开发规则

## 开始前

1. 先阅读 `README.md`、`docs/04-api-contract.md` 和 `docs/07-team-ownership.md`。
2. 确认当前分支、任务允许修改的目录和已有未提交改动。
3. 只做任务需要的局部实现，不进行无关重构。

## 不可违反的规则

- 不删除已有功能，不修改其他成员已经完成的模块。
- 不私自改变 API 字段名称、类型或统一响应格式。
- 公共代码不得写死某个具体品类或商品。
- TypeScript 禁止滥用 `any`；后端输入输出必须使用 Pydantic。
- 外部调用必须有超时和异常处理；`source_ids` 为空的结论不得输出。
- 配置和密钥只能来自环境变量，禁止提交真实密钥。
- 成员 A 是 `data/mock/` 的唯一直接维护者；成员 C 不自行修改 Mock 数据结构。
- 修改完成后必须运行对应测试或构建，并说明未完成项和风险。

## 分工边界

详见 `docs/07-team-ownership.md`。共享入口、API 合同和根配置由成员 D 或项目负责人维护。

